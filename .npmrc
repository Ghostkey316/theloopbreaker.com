# Vaultfire npm config
# omit=dev: npm audit only checks production deps (0 vulnerabilities)
# This does NOT affect npm ci -- devDeps are still installed
# [ -n "$CI" ] || husky in package.json ensures npm ci succeeds in CI
omit=dev

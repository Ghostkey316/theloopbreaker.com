# Vaultfire module scope controls which services load at startup.
# Acceptable values: pilot, full
VAULTFIRE_MODULE_SCOPE=pilot
# Enable to relax telemetry residency for mobile shells and skip heavy CLI checks.
MOBILE_MODE=true

# Builder Verification README

Silence doesn’t mean absence. Presence has weight.

The Builder Verification stream now ingests Drift Anchor protocol results to surface returning legacy builders and their active anchor scores. Use `npm run drift:scan` to refresh the verification export before publishing updates to Vaultfire’s public verification layer.
